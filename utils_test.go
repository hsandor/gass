package gass

/*
import (
	"testing"
)


func TestIndentLevelCalculation(t *testing.T) {
	// https://docs.python.org/release/2.5.1/ref/indentation.html
	cases := []struct {
		level int
		line  string
	}{
		{0, ""}, {1, " "}, {0, "\ra"}, {9, "\t \r"}, {0, "a"},
		{1, " a"}, {1, " \ra"}, {2, " \r a"}, {8, "\ta"},
	}

	for i, c := range cases {
		if l := calcIndentLevel(c.line); l != c.level {
			t.Log("indentation level mismatch, test:", i, ", expected:", c.level, " got:", l, " for line:'"+c.line+"'")
			t.Fail()
		}
	}
}
*/
